# OuterTune 二次开发规则

## 在线音乐源必须经过统一边界

- **触发信号**：新增在线搜索、播放地址解析、歌词或封面来源。
- **根因/约束**：原项目的 YouTube/InnerTube 类型渗透到 UI、队列和播放服务，直接替换单个 API 会继续产生跨层耦合。
- **正确做法**：UI 与播放队列只使用 `MediaMetadata`；平台专属参数放入 `source`、`sourceId`、`sourceData`，由 `source/lx` 适配层完成搜索、播放 URL 和下载 URL 解析；这三个字段必须同步持久化到 `SongEntity`，避免队列恢复后丢失路由信息。
- **验证方式**：在线搜索结果可转换为 `MediaMetadata`，播放与下载服务均根据 `source` 路由，不新增 `com.zionhuang.innertube` 依赖；保存并恢复队列后仍保留在线源参数。
- **适用范围**：所有新增或替换的在线音乐源。

## 禁止恢复 YouTube/InnerTube 构建依赖

- **触发信号**：修改 `settings.gradle.kts`、`app/build.gradle.kts`、在线播放、账号或同步功能。
- **根因/约束**：产品已经移除 YouTube 登录、浏览、推荐、同步和播放解析链路，重新引入 `:innertube` 会恢复旧平台耦合并产生两套在线数据模型。
- **正确做法**：`app` 不得依赖或引用 `:innertube`、`com.zionhuang.innertube`、`YTPlayerUtils` 或 YouTube 队列；新增在线能力统一扩展 `source/lx` 边界。
- **验证方式**：`settings.gradle.kts` 不包含 `include(":innertube")`，`app/build.gradle.kts` 不依赖该模块，应用源码残留扫描为空，`:app:compileCoreDebugKotlin` 通过。
- **适用范围**：应用模块、Gradle 构建图和所有在线音乐功能。

## 洛雪自定义源的职责边界

- **触发信号**：导入或执行洛雪自定义音源脚本。
- **根因/约束**：洛雪自定义源主要提供 `musicUrl`、`lyric`、`pic`，不保证提供搜索；脚本依赖 `lx` API 和 QuickJS 运行环境。
- **正确做法**：搜索元数据由独立客户端提供，播放 URL 由 QuickJS 兼容运行时调用音源脚本解析；项目可携带一个明确标注的推荐音源用于开箱测试，用户导入的替换脚本仅保存在本地 DataStore。
- **验证方式**：未导入脚本时给出明确配置错误；导入后可初始化脚本并通过 `musicUrl` action 返回 HTTP(S) 地址。
- **适用范围**：洛雪音源设置、搜索、播放、歌词与封面扩展。

## Media3 fork 的 AGP 必须与主工程一致

- **触发信号**：Gradle 报告同一构建混用 AGP 8.13.2 与 9.0.0。
- **根因/约束**：上游 `media` composite build 使用 AGP 9.0.0，主工程使用 AGP 8.13.2，Gradle 不允许共同解析。
- **正确做法**：保留本地 `media` dependency substitution（项目依赖其中的定制 API），并将 `media/build.gradle` 的 AGP 版本对齐主工程。
- **验证方式**：`:app:compileCoreDebugKotlin` 不出现 `AgpVersionCompatibilityRule`，且 Media3 定制 override 可以编译。
- **适用范围**：当前二次开发分支的 Android 构建。
- 不主动打包APK，如果打包apk，需要增加版本号，只需要打包出v8a版本的包

## 本地 Android 构建使用临时 JDK 与 SDK

- **触发信号**：本地执行 Gradle 编译或打包，且系统未配置 `JAVA_HOME`、`ANDROID_HOME`。
- **根因/约束**：`media` composite build 不会继承根工程的 `local.properties`，而遗留 Kotlin 守护进程可能占用增量编译缓存。
- **正确做法**：使用 `E:\code\jdk21-temp\jdk-21.0.12` 作为 `JAVA_HOME`，使用 `E:\code\android-sdk-temp` 作为 `ANDROID_HOME` 与 `ANDROID_SDK_ROOT`；PowerShell 中执行 `./gradlew.bat <task> --no-daemon "-Dkotlin.incremental=false"`。完成后执行 `./gradlew.bat --stop` 释放 Gradle/Kotlin JVM。
- **验证方式**：`:app:compileCoreDebugKotlin` 输出 `BUILD SUCCESSFUL`；构建完成后 `./gradlew.bat --status` 不显示运行中的 daemon。
- **适用范围**：本机的编译、打包与 Gradle 故障排查。

## 首页与界面本地化保持精简

- **触发信号**：修改首页推荐内容、界面字体或新增语言资源。
- **根因/约束**：当前产品面向中文用户，首页承担快速续播入口，不再承载 YouTube 推荐、探索或随机播放功能。
- **正确做法**：首页只展示按时间倒序、去重后的最近播放；Compose 主题统一使用 Android 系统无衬线字体；应用默认资源和 locale 配置只保留简体中文。
- **验证方式**：`:app:compileCoreDebugKotlin` 通过，生成的 locale config 仅包含 `zh-CN`，首页代码不依赖 `YouTube` 或 `innertube` 类型。
- **适用范围**：应用首页、全局主题和 `app` 模块的用户界面资源。

## 在线搜索不得等待逐项媒体资源

- **触发信号**：搜索结果需要封面、歌词或播放地址等附加数据。
- **根因/约束**：搜索后逐首串行请求封面会把一次查询放大为数十次网络往返，严重拖慢首屏结果。
- **正确做法**：搜索阶段只请求并解析结果元数据及响应自带的封面 URL；文字结果立即显示，列表仅对可见项异步加载封面，不额外逐首调用封面接口；播放前仅为当前歌曲补充缺失封面和播放地址；短时间重复查询使用有容量上限的内存缓存。
- **验证方式**：搜索路径不调用逐首封面接口，`limit` 保持 15，`:app:compileCoreDebugKotlin` 通过，播放队列中的当前歌曲包含封面地址。
- **适用范围**：所有洛雪在线搜索源和后续新增的在线音乐源。

## 多音源搜索保持统一播放参数

- **触发信号**：新增或修改酷狗、网易云、QQ 音乐、酷我等搜索入口。
- **根因/约束**：各平台返回字段不同，但导入的洛雪 JS 音源依赖来源标识和原始歌曲参数解析播放地址；只统一显示字段会导致搜索可用、播放失败。
- **正确做法**：所有搜索客户端转换为 `LxMusicInfo`，保留 `source`、`songmid` 及平台专属的 `hash`、`mid`、`musicId`，再经 `MediaMetadata.sourceData` 传给洛雪运行时；切换音源只取消并替换在线结果，不能影响置顶的本地结果。
- **验证方式**：四个音源均能返回歌曲标题、歌手、时长；切换后播放队列的歌曲保留对应 `source` 与 `sourceData`；`:app:compileCoreDebugKotlin` 通过。
- **适用范围**：`source/lx` 搜索适配、在线搜索结果页和播放队列。

## 下载音质与播放音质必须隔离

- **触发信号**：修改洛雪音源解析、下载任务或下载目录保存逻辑。
- **根因/约束**：下载与播放共用一次播放地址解析时，会让用户的临时播放音质意外决定离线文件大小和格式。
- **正确做法**：下载使用独立的 `DownloadQualityKey` 覆盖洛雪解析音质，并直接保存到用户选择的下载目录；128k/320k 使用 `.mp3`，FLAC/Hi-Res 使用 `.flac`。
- **验证方式**：修改下载音质不改变播放音质；下载解析请求携带下载配置；写入目录与设置一致，文件扩展名与所选等级匹配。
- **适用范围**：下载设置、`DownloadUtil`、洛雪运行时和下载目录管理器。

## 搜索本地优先与播放记录完整性

- **触发信号**：修改统一搜索结果页或最近播放展示。
- **根因/约束**：本地文件无需网络即可播放，且音频标签可能缺少时长；若只展示在线结果或仅以元数据时长计算播放比例，本地歌曲会被延后或遗漏。
- **正确做法**：统一搜索并发查询本地歌曲，固定置于在线结果之前；播放记录写入前确保队列歌曲已持久化，优先用元数据时长、再回退播放器时间线时长，均缺失时按有效播放记录处理。
- **验证方式**：按歌名或歌手搜索时本地歌曲位于第一组；播放本地歌曲后首页最近播放和历史页均出现该歌曲。
- **适用范围**：搜索结果页、播放统计回调与最近播放入口。

## 本地媒体元数据优先

- **触发信号**：修改本地歌曲封面或歌词展示。
- **根因/约束**：本地歌曲可能未写入独立封面路径，并且内嵌歌词比在线搜索更准确；在线请求还会增加播放后的等待时间。
- **正确做法**：列表封面为空时使用本地音频文件作为内嵌封面读取源；歌词解析先读取文件内的 `LYRICS` 标签，命中后直接展示且不访问在线歌词源。
- **验证方式**：无独立封面路径的本地歌曲在最近播放与歌曲列表显示内嵌封面；含内嵌歌词的本地歌曲播放时不触发在线歌词请求。
- **适用范围**：本地扫描、歌曲列表、播放器歌词与最近播放。

## 主导航保持固定抽屉结构

- **触发信号**：调整首页、主导航入口或播放器底部安全区。
- **根因/约束**：底部标签栏与迷你播放器同时占用屏幕底部，且可配置标签会使入口分散、默认页不稳定。
- **正确做法**：主入口固定为左侧抽屉中的歌曲、歌单、设置和关于；应用始终从歌曲页进入，歌曲页按喜欢、媒体库、已下载和最近播放分组，默认媒体库；不恢复底部标签栏。
- **验证方式**：启动后显示歌曲页与媒体库筛选；抽屉仅显示四个主入口；迷你播放器上方不留底部导航空白。
- **适用范围**：`MainActivity`、主导航组件、歌曲页与界面设置。
